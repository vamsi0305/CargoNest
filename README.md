# CargoNest

CargoNest is scaffolded as:

- `frontend/`: React + Vite + TypeScript
- `backend/`: FastAPI + SQLModel

Current first slice:

- operational dashboard shell
- purchase order listing and detail view
- FastAPI health and purchase order endpoints
- Supabase-ready environment placeholders
